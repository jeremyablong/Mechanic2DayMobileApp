import React, { Fragment } from 'react';
import ReferralSystemMainHelper from "../../components/referral/main.js";

const ReferralSystemMainPage = (props) => {
    return (
        <Fragment>
            <ReferralSystemMainHelper props={props} />
        </Fragment>
    );
}
export default ReferralSystemMainPage;