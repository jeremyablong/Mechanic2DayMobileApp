import React, { Fragment } from 'react';
import PayoutMethodAddNewPayoutHelper from "../../../../../components/account/payments/payouts/addNew/addNewPayoutMethod.js";

const PayoutMethodAddNewPayoutPage = (props) => {
    return (
        <Fragment>
            <PayoutMethodAddNewPayoutHelper props={props} />
        </Fragment>
    );
}
export default PayoutMethodAddNewPayoutPage;