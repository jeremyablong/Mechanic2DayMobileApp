import React, { Fragment } from "react";
import ProfileMainHelper from "../../../components/account/profile/main.js";

const ProfileMainPage = (props) => {
    return (
        <Fragment>
            <ProfileMainHelper props={props} />
        </Fragment>
    );
}
export default ProfileMainPage;