import React, { Fragment } from "react";
import MapViewAllListingsHelper from "../../components/listings/main.js";

const MapViewAllListingsPage = (props) => {
    return (
        <Fragment>
            <MapViewAllListingsHelper props={props} />
        </Fragment>
    );
}
export default MapViewAllListingsPage;